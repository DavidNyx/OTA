<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ota">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ota1_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ota1_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTA">
        <activity android:name=".EditGrade"></activity>
        <activity android:name=".AttendanceMenu"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".EditAttendance" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ParentsActivity" />
        <activity android:name=".AdministratorActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".Grade.grade" />
        <activity android:name=".Email" />
        <activity android:name=".Fee.FeeActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".schedule" />
    </application>

</manifest>